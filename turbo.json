{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "dev": {
      "cache": false,
      "dotEnv": [".env.local", ".env"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "dotEnv": [".env.local", ".env"]
    },
    "start": {
      "dependsOn": ["^build"]
    }
  },
  "globalDotEnv": [".env.local", ".env"]
}
